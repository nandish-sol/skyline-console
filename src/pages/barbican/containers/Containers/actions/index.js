import Delete from './Delete';

const actionConfigs = {
  rowActions: {
    moreActions: [
      {
        action: Delete,
      },
    ],
  },
  batchActions: [Delete],
};

export default actionConfigs;
